version: "3.9"
services:
  client:
    build: ./client
    ports:
      - 8081:8081
    container_name: client-prod
    depends_on:
      - server
  server:
    build: ./server
    ports:
      - 8080:8080
    container_name: server-prod